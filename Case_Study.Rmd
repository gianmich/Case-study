Case study
========================================================

Das ist eine Übung damit ich "tidy data" lerne.


First step is to load the libraries we need:

```{r}
library(reshape2)
library(ggplot2)
library(plyr)
library(stringr)
library(MASS)
```

Place xtable.r in the followin Directory. Dann habe ich die Package xtable installiert

source("~/Documents/Doktorarbeit/xtable.r")

Download .csv Datei



You can also embed plots, for example:

```{r fig.width=7, fig.height=6}
plot(cars)
```

